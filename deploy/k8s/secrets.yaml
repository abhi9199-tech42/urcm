apiVersion: v1
kind: Secret
metadata:
  name: urcm-secrets
type: Opaque
# Create secrets out-of-band:
#   kubectl create secret generic urcm-secrets \
#     --from-literal=admin_key=$(openssl rand -hex 32) \
#     --from-literal=metrics_token=$(openssl rand -hex 32)
